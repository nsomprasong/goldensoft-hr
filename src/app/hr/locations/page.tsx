import OperationsWorkspace from "@/components/hr/operations-workspace"; import HrShell from "@/components/hr-shell"; import { requireHrPage } from "@/lib/hr/guards"; import { HR_PERMISSIONS } from "@/lib/hr/permissions";
export const dynamic = "force-dynamic";
export default async function LocationsPage() { const ctx = await requireHrPage({ permission: HR_PERMISSIONS.locationManage }); return <HrShell ctx={ctx}><OperationsWorkspace title="สถานที่ทำงาน" description="จุดลงเวลาและขอบเขตพื้นที่ขององค์กร" emptyMessage="ยังไม่มีสถานที่ทำงาน" endpoint="/api/hr/work-locations" actions={[{ label: "เพิ่มสถานที่", action: "create" }]} /></HrShell>; }
